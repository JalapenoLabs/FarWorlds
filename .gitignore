node_modules/
dist/
.yarn/cache/
.yarn/install-state.gz
.vite/
*.log

# The archived original extension is closed source. It stays local as a behavioural reference and is never
# committed or redistributed.
archive/

# Store zips are built per release and attached to GitHub releases, never committed.
release/

# Raw captures stay local; only the processed store images are committed.
store/screenshots/Alex.png
